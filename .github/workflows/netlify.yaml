name: Trigger Netlify Build
on:
  schedule:
    - cron: '* * * * *' # Every minute
jobs:
  build:
    name: Request Netlify Webhook
    runs-on: ubuntu-latest
    steps:
      - name: POST to Build Hook
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/61dce4a7b61e4b668399a94d
